/*
import {CommandsControllerInterface} from "./commands-controller.interface";

export class CommandsController {
    @Command('start')
    async start(ctx: Context) {
        await ctx.reply('Welcome! Use /help to see available commands.');
    }

    @Command('help')
    async help(ctx: Context) {
        await ctx.reply('Available commands: /start, /help, /rewrite');
    }

    @Command('rewrite')
    async rewrite(ctx: Context) {
        await ctx.reply('This is the /rewrite command!');
    }
}*/
